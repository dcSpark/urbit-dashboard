import React, { useState, useRef } from "react";

export default function Navbar(){
    return(
        <nav>
            <div className="navbar-title">
             <h1>Urbit Dashboard</h1>
            </div>
            <div className="navbar-buttons">
                {/* <button>one</button>
                <button>two</button> */}
            </div>
        </nav>
    )
}